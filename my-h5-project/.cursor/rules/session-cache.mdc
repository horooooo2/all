---
description: 临时 UI/业务缓存使用 sessionStorage，关闭标签页后重置
alwaysApply: true
---

# 会话级缓存（sessionStorage）

除明确需要跨会话保留的数据外，**所有缓存**一律用 `src/utils/sessionCache.js`，不要用 `localStorage` 直接读写。

## 行为

- 关闭浏览器标签页后缓存自动清空，下次打开重新展示/初始化。
- 同标签页内刷新、路由跳转仍保留缓存。

## 必须用 sessionCache 的场景

- 弹窗/浮层关闭状态（如 APP 下载条）
- 表单草稿、面板折叠、筛选条件等 UI 状态
- 游戏内自定义筹码、连号区间等本地偏好
- 仅当前会话有效的 mock/临时列表数据

## 继续用 localStorage 的例外

- 登录 token、用户信息（Pinia persist、`request.js`）
- 语言偏好：`locale`（跨访问保持）

## 记住密码（localStorage）

- `loginRemember.js`：勾选记住密码后写入 localStorage，关闭标签页仍保留；清除站点数据或取消勾选时失效
- 退出登录、修改登录密码、40101 跳转登录等**不**主动清除；仅用户取消勾选「记住密码」时清除

## 写法

```js
import {
  getSessionItem,
  setSessionItem,
  getSessionJSON,
  setSessionJSON,
  migrateLocalToSession
} from '@/utils/sessionCache'

migrateLocalToSession('my_cache_key') // 兼容旧 localStorage，可选
setSessionItem('my_cache_key', '1')
const dismissed = getSessionItem('my_cache_key') === '1'
```

新增缓存时优先封装到独立 util（如 `withdrawalAccounts.js`），内部统一走 `sessionCache`。
